class adminReg{
    constructor(name,userid,password,reenterPassword,contact_no){
           this.name = name;
          // if(password==reenterPassword)
           // this.login = {userid ,password};
           this.userid=userid;
           this.password = password;
           this.contact_no=contact_no;

    }
}
module.exports = adminReg;